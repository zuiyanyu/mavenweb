package scala集合.Stream流

/**
  * stream是一个集合，这个集合可以用于存储无穷多个元素，但是这无穷个元素不会一次性
  * 生产出来，而是需要用到多大的区间，就会动态的生产，末尾元素遵循lazy规则(即：
  * 要使用结果才会进行计算)
  */
object Stream流 {
  def main(args: Array[String]): Unit = {

  }
  //TODO 1.  声明方式
  def define(): Unit ={
    println("==========TODO 1.  声明方式==============")

  }

}
